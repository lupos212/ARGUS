<Page
    x:Class="ARGUS.Views.ConfidentialInformantPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ARGUS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Back Button -->
            <Button Content="Back" Click="BackButton_Click" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Column="0" Margin="10"/>

            <!-- Centered Title -->
            <TextBlock Text="C. I.   D A T A B A S E" Style="{StaticResource PageTitleStyle}" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Column="1" Margin="10,0,0,0"/>

            <!-- Empty Column for Spacing -->
            <Grid Grid.Column="2"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Your main content here -->
        </Grid>
    </Grid>
</Page>